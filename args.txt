
First run:
get-original-file-info.py -i test-dataset.csv -o odfi
This produces file, odfi.pickle: Has the line count and column count of test-dataset.csv.

For spliting the dataset；
split.py -i test-dataset.csv --odfi odfi.pickle --train 0.8 --test 0.2

After generating two files containing the datasets for training and testing datasets, we will run；
create-sets.py -i test-dataset.csv --odfi odfi.pickle --trc 2 --vrc 4 --extval --cc 4 --cn 1 --oc 2 --tsc 4 --dataset 0 train_index.txt


1. features (asc order)
2. two ways: add ex in the first(--extval) or second command(--extval 0.8)
3. size of val set, --vrc 4,  
4. mvs always True, same size as training, same as tsc

3&4. 1. 2.